---
description: Regenerate CODE_INDEX.md — rebuild the function/heading line-number index for this project
---

Invoke the code-index-md skill.

Run `bash hooks/reindex.sh` (or `bash scripts/reindex.sh` if configured locally) to regenerate `CODE_INDEX.md`, then confirm it was updated by checking the timestamp at the top of the file.
